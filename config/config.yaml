# ============================================
# A股量化选股系统 - 主配置文件
# ============================================
# 注意：此文件包含敏感信息时不应提交到Git仓库
# 请从 config.yaml.template 复制并填入你的配置

data_dir: data

dingtalk:
  webhook_url: ""
  secret: ""

schedule:
  time: "15:05"

update:
  lookback_days: 10
  skip_failed: true
