# QSelect 云阶决策台设计规范

更新时间：2026-08-11。具体实现以 `frontend/src/index.css`、Astryx Neutral 主题和现有组件为准。

## 产品结果契约

当 A 股研究用户查看每日收盘结果时，帮助他先确认“数据是否完整、正式动作是什么”，再判断“哪些策略的前向证据更可靠、模型今天学到了什么”，同时保证每个结论来自当前不可变快照、正式决策账本和可追溯复盘证据。

- 目标用户：以云阶为首要策略、需要快速作出研究判断的单一专业用户。
- 核心任务：先看正式收盘动作和云阶结构信号，再看 28 策略的 T+5 影子排名、样本可信度、风险和每日复盘结论。
- 期望情绪：清楚、确定、克制、可控，不制造“信息很多但无法行动”的压力。
- 关键约束：不虚构信号、行业或复盘数据；不采集、不展示、不使用消息面；云阶命中只代表结构确认，买入/观察/回避只认 canonical decision ledger；T+5 固定为主评估窗口；样本不足显示“预热中”；AI 只解释已算出的证据，不能改排名、权重或动作；正式快照长期保留。

## 页面与优先级

### 决策台 `/stocks`

- P0：今日云阶结构信号数量、市场温度、候选优先级、价格、行业、行业热度/排名、正式账本动作。
- P1：日/周/月 K、云阶三段证据、证据优先级、AI 解释、结构失效条件、板块共振。
- P2：图表设置、历史记录、同板块对比、展示筛选、板块全景，全部渐进披露。

### 复盘 `/review`

- P0：今日确定性结论、当前 snapshot/日期、模型与 AI 状态、28 策略总数、当日命中和达到资格的策略数。
- P1：单一全策略榜单，以 T+5 的贝叶斯胜率、Wilson 下界、日均净收益、CVaR10、成熟信号日和样本数解释排名；T+1/T+10/T+20 渐进披露。
- 样本不成熟时显示“预热中”，不得用零值或估算收益伪装完整复盘，也不得展示虚假冠军。

### 后台 `/admin`

- 查看行情、契约、快照、云阶发布、AI、任务和来源。
- 前台正式动作只认 canonical decision ledger；分层模型和全策略评分保留为影子证据，不覆盖正式动作。
- 写操作必须有真实权限；当前只读页面不提供可误触的假按钮。

## 视觉语法

- 画布：`#f6f5f4`，面板：白色，主文字：`#191918`，次级文字：`#615d59`，弱文字：`#a39e98`。
- 单一交互强调色：`#0075de`；A 股语义保持红涨、绿跌。
- 字体：Inter + 中文系统无衬线；数字使用 tabular figures；代码与快照使用等宽字体。
- 桌面决策台最大宽度 1440px；顶部导航 52px；候选清单与当前研究结论采用 420px + 自适应右栏的主从工作面。
- 圆角、边框和阴影保持轻量，优先用留白与排版建立层级。
- 不使用渐变、玻璃拟态、装饰性大标题、无意义图标或假可视化。

## 图表与数据

- K 线使用 ECharts，红涨绿跌；日/周/月周期均来自真实接口。
- MA5/10/20/60、多空线、趋势线、信号点和 KDJ/成交量设置均可操作。
- 图表必须显示数据日期、收盘价和周期；数据错误时提供明确重试路径。
- 行业迷你趋势和进度条只使用接口返回的热度、广度数据。
- 候选优先级固定为“云阶结构 70% + 板块共振 30%”；消息与 AI 均不能参与排序。

## 响应式与动效

- 1600×1000 与 390×844 保持同一信息树；移动端隐藏非关键系统状态，保留三条主导航和核心决策。
- 手机端先展示候选清单，后展示当前候选的详情、图表、证据链和执行建议，不出现页面级横向滚动。
- 状态展开只使用 `opacity` / `transform`，时长和缓动来自项目 token。
- `prefers-reduced-motion: reduce` 下移除非必要动效，同时保留状态反馈。

## 禁止项

- 首页堆叠策略、因子、模型或行业全量信息。
- 把“结构已确认”“观察”或“研究候选”冒充买点，或绕过正式决策账本输出动作。
- AI 自主挑票、增删股票、修改排名或给出无证据收益保证。
- 复盘补假数据、行业缺失时猜测分类、用消息或 AI 改写云阶候选与排序。
- 可见但不可工作的筛选、标签、图表设置、链接和按钮。

## 验收入口

```bash
cd frontend
npm run lint
npm run build
```

再用 Codex 内置浏览器检查 `/stocks`、`/review`、`/admin`，验证候选切换、日/周/月 K、图表设置、筛选、板块全景、复盘空态和后台审计展开；最后用 1600×1000 同尺寸截图与参考前端并排比较，并检查 390×844 窄屏。
