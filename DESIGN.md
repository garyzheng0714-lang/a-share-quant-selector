# 前端设计规范：亮色数据研究工作台

更新时间：2026-08-05。实现以 Astryx 0.2、`frontend/src/index.css` 与 `frontend/src/lib/tokens.ts` 为准。

## 方向

亮色、数据优先的个人研究工作台。界面首先说明数据池、条件、筛选过程、结果与证据，再提供操作；不把量化研究做成装饰性交易大屏。

策略是可组合的数据条件。用户从每日股票池出发，把策略加入组合区，以明确的 AND 逻辑得到真实交集；点击任一策略时，检查器切换为该策略的独立命中结果。界面不是组件集合，而是可验证、可恢复的状态转换系统。

中文 A 股语义保持红涨、绿跌。颜色不能是唯一状态线索。

## 色彩与层级

- 只提供亮色模式；画布使用浅中性灰，数据面板使用白色；
- 主文字近黑，次级和弱化文字保持清晰的两级灰度；
- 主强调使用克制蓝色，只用于关键动作、焦点、当前选择和拖放位置；
- 上涨/买入风险语义：红；下跌/负向语义：绿，遵循 A 股用户习惯；
- 结构依靠 AppShell、连续区域、1px 边界和语义留白，不使用暗色、渐变、玻璃拟态或卡片瀑布流。

## 排版

- 正文使用系统无衬线中文字体；
- 数据、代码、关键数字和少量标题可用等宽字体；
- 页面标题 24–30px，区块标题 16–18px，正文 14px，辅助信息 12px；
- 数字使用 tabular figures；单位、日期和 as-of 必须清晰。

## 布局与组件

- 应用根框架使用 Astryx `AppShell` 与 `TopNav`；主动作、输入、选择器、状态、表格、空状态和弹窗优先使用 Astryx 原生组件；
- `/stocks` 是策略工作台权威入口（2026-09 按「选股工作台 v3」设计稿重做）。桌面为两栏：左栏因子库（搜索、只看已验证、按系分组、每行当日命中数），右栏结果；顶部一排常用组合按钮带当日命中数，点一下条件和结果同时切换；
- 点一个因子结果立刻出来，不需要“运行”；点第二个才出现「全部满足 / 任一满足」切换，没有分组、排除组、筛选漏斗和拖拽排序；
- 所选因子、且/或关系和日期写入 URL，刷新与返回不得丢失研究上下文；
- 结果列固定为：股票、20 日迷你走势、最新价/涨跌、J/RSI、行业·热度、流通市值、命中因子、连命中天数、样本外胜率；排序只提供连命中 / 胜率 / 涨跌幅三档；
- 手机使用固定底部导航，内容需预留 safe area；
- 手机按“常用组合 → 因子库（固定高度内滚动）→ 结果列表”纵向排列；
- 数据表、K 线和密集列表优先信息密度；容器圆角控制在 6–10px；
- `/sectors` 是单页板块工作台，同屏回答排名、阶段、真实热度趋势、候选、证据缺口与系统状态；
- 当前候选页只回答动作、原因、时点和风险，不能重复制造另一套推荐结论；
- 页面不能同时出现互相冲突的「今日一票」或重复结论。

## 图表

- ECharts 使用统一亮色图表 token；
- K 线红涨绿跌，均线颜色稳定；
- 日线与周线必须明确标注周期、as-of、当前周是否未完成；
- 信号标记不能遮挡价格，tooltip 提供键盘/触摸可达的等价信息；
- 没有数据、数据过期或模型未验证时显示原因，不画误导性的零值。

## 动效与响应式

- hover/press 只做轻微位移或缩放；
- 页面与列表进入动画总延迟受限，不能拖慢行情阅读；
- 尊重 `prefers-reduced-motion`；
- 360px 与 390px 视口不得横向溢出，底部导航不得遮挡最后一行；
- 桌面、触摸与键盘都必须能完成导航和主要操作。

## 禁止项

- 把 20+ 因子卡堆到首页；
- 暗黑模式或根据系统偏好自动切换暗色；
- 用 React Flow、Blockly 等重型节点编辑器表达简单 AND 条件；
- 只有拖拽而没有点击、键盘或触屏替代；
- 用部分成功的策略结果伪装完整交集；
- 用 AI 文案覆盖规则动作；
- 用综合分隐藏数据缺失或模型未验证；
- 无意义渐变、玻璃拟态、重阴影或持续动画；
- 把回测胜率直接渲染为「必买」。

## 验收

```bash
cd frontend
npm run lint
npm run build
```

再用 `frontend/scripts/mobile-shot.mjs` 或浏览器检查 `/sectors`、`/stocks`、`/review`、个股日/周 K，确认无横向溢出、数据时点清晰、动作与风险不冲突。
