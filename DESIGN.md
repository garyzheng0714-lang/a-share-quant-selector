# 前端设计规范

设计依据：[userinterface-wiki Skill](/Users/simba/.agents/skills/userinterface-wiki/SKILL.md)（`raphaelsalaja/userinterface-wiki`），组件层使用 Astryx 0.2（`@astryxdesign/core`）。本文只记录本项目的具体决策。

## 方向

亮色、数据优先的选股工作台。只有两个页面，不做仪表盘、不做装饰性大屏。A 股语义红涨绿跌，颜色不是唯一状态线索。数字用 tabular figures，日期和 as-of 必须清楚。

## `/select` 选股工作台

- 顶部一排常用组合按钮带当日命中数，点一下条件和结果同时切换。
- 左栏因子库：搜索、按系分组、每行当日命中数；点一个因子结果立刻出来，不需要「运行」。
- 右栏结果：已选因子 token（可移除），选到第二个才出现「全部满足 / 任一满足」；结果表列固定为：股票、20 日迷你走势、最新价/涨跌、J/RSI、行业、流通市值、命中因子、连命中天数；排序只有连命中 / 涨跌幅。
- 所选因子、且/或、日期写入 URL，刷新不丢。
- 手机：常用组合 → 因子库（固定高度内滚动）→ 结果列表。

## `/stock/:code`

日 K / 周 K 切换；日线带 KDJ、知行白线/黄线，周线带 MA5/10/20/60；K 线下方金点标记该票历史因子命中日；左右切换来自结果列表的上一只/下一只。

## 禁止

- 卡片瀑布流、暗色主题、渐变、玻璃拟态；
- 在页面上出现胜率、评级、推荐、买卖动作；
- 用部分结果冒充完整组合结果。
